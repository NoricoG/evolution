{
    "name": "evolution",
    "scripts": {
        "build": "tsc && esbuild src/ui/ui.ts --bundle --outfile=compiled/script.js",
        "typecheck": "tsc --watch",
        "watch": "esbuild src/ui/ui.ts --bundle --outfile=compiled/script.js --watch"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}
